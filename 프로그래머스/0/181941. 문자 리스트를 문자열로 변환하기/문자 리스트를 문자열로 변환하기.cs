using System;

public class Solution {
    public string solution(string[] arr) {
        string result = "";
        for(int i = 0 ; i<arr.Length; i++){
            result += arr[i];
        }
        return result;
    }
}